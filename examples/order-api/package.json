{
  "name": "tsoap-example-order-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsoap generate --input order.wsdl --output ./generated",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "typed-soap": "workspace:*"
  },
  "devDependencies": {
    "tsoap-cli": "workspace:*",
    "@types/node": "^18.0.0",
    "typescript": "^5.9.2"
  }
}
